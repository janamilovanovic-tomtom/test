name: 'Sharepoint Sync'

on:
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:

      - name: Cloning repo # This step is required
        uses: actions/checkout@v2

      - name: Publish to Sharepoint
        uses: obrassard/action-sharepoint-publish@v1.0.1
        with:
          site_url: 'https://tomtominternational.sharepoint.com/sites/nav-dx-guidance'
          library_folder: 'Shared Documents/nie-ux-spec'
          sharepoint_user: ${{ secrets.CLIENT_ID }}
          sharepoint_password: ${{ secrets.CLIENT_SECRET }}